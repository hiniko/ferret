services:
  postgres:
    image: postgres:17
    environment:
      POSTGRES_PASSWORD: ferret
      POSTGRES_DB: ferret_samples
    ports: ["5433:5432"]
    volumes: [pgdata:/var/lib/postgresql/data]

volumes:
  pgdata:
